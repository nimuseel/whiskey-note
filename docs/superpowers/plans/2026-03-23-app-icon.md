# App Icon Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 글렌케언 글라스 디자인의 새 앱 아이콘 PNG를 생성해 Xcode 에셋 카탈로그에 교체한다.

**Architecture:** 마스터 SVG(1024×1024)를 하나 만들고, ImageMagick(`convert`)으로 모든 해상도 PNG를 일괄 생성한 뒤 기존 파일을 덮어쓴다. rsvg-convert는 없으므로 ImageMagick을 사용한다.

**Tech Stack:** SVG, ImageMagick 7 (`convert`), bash

---

### Task 1: 마스터 SVG 생성

**Files:**
- Create: `docs/superpowers/icon/icon-master.svg`

- [ ] **Step 1: 디렉토리 생성**

```bash
mkdir -p "/Users/sumin/dev/ios/whiskey-note/docs/superpowers/icon"
```

- [ ] **Step 2: 마스터 SVG 파일 작성**

`docs/superpowers/icon/icon-master.svg`를 아래 내용으로 생성한다.
캔버스: 1024×1024. 글라스 패스는 72×72 기준을 1024/72 ≈ 14.22배 스케일해서 적용한다.

```xml
<svg width="1024" height="1024" viewBox="0 0 72 72" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <clipPath id="glassClip">
      <path d="M20,8 C16,14 14,24 14,38 C14,48 22,54 30,55 L30,60 L22,60 L22,66 L50,66 L50,60 L42,60 L42,55 C50,54 58,48 58,38 C58,24 56,14 52,8 Z"/>
    </clipPath>
  </defs>

  <!-- 배경 -->
  <rect width="72" height="72" fill="#2d1e0a"/>

  <!-- 액체 (글라스 내부 클리핑) -->
  <rect x="0" y="38" width="72" height="30" fill="#e8a838" fill-opacity="0.9" clip-path="url(#glassClip)"/>

  <!-- 액체 표면 라인 -->
  <path d="M15,38 Q36,35 57,38"
        stroke="#faf8f5" stroke-width="1.2" stroke-opacity="0.45"
        fill="none" clip-path="url(#glassClip)"/>

  <!-- 글라스 아웃라인 -->
  <path d="M20,8 C16,14 14,24 14,38 C14,48 22,54 30,55 L30,60 L22,60 L22,66 L50,66 L50,60 L42,60 L42,55 C50,54 58,48 58,38 C58,24 56,14 52,8 Z"
        fill="none"
        stroke="#faf8f5" stroke-opacity="0.75"
        stroke-width="1.8"
        stroke-linejoin="round"/>
</svg>
```

- [ ] **Step 3: SVG 미리보기 확인**

```bash
qlmanage -p "/Users/sumin/dev/ios/whiskey-note/docs/superpowers/icon/icon-master.svg" 2>/dev/null &
```

글라스 형태, 액체 레벨, 색상이 스펙과 일치하는지 눈으로 확인.
문제 있으면 SVG 수정 후 다시 확인.

---

### Task 2: PNG 일괄 생성

**Files:**
- Create: `docs/superpowers/icon/generate.sh`
- Create: `docs/superpowers/icon/{size}.png` (11개)

- [ ] **Step 1: 생성 스크립트 작성**

`docs/superpowers/icon/generate.sh`:

```bash
#!/bin/bash
set -e

SVG="/Users/sumin/dev/ios/whiskey-note/docs/superpowers/icon/icon-master.svg"
OUT="/Users/sumin/dev/ios/whiskey-note/docs/superpowers/icon"

for size in 1024 180 120 87 80 60 58 57 40 29 114; do
  echo "Generating ${size}x${size}..."
  magick \
    -background '#2d1e0a' \
    -density 300 \
    "$SVG" \
    -resize "${size}x${size}" \
    -flatten \
    "${OUT}/${size}.png"
done

echo "Done. Verifying dimensions..."
for size in 1024 180 120 87 80 60 58 57 40 29 114; do
  actual=$(magick identify -format "%wx%h" "${OUT}/${size}.png")
  expected="${size}x${size}"
  if [ "$actual" = "$expected" ]; then
    echo "  ✓ ${size}.png"
  else
    echo "  ✗ ${size}.png — got $actual, expected $expected"
  fi
done
```

- [ ] **Step 2: 스크립트 실행**

```bash
chmod +x "/Users/sumin/dev/ios/whiskey-note/docs/superpowers/icon/generate.sh"
bash "/Users/sumin/dev/ios/whiskey-note/docs/superpowers/icon/generate.sh"
```

Expected: 11개 PNG 파일 생성 완료 메시지.

- [ ] **Step 3: 1024.png 시각 확인**

```bash
open "/Users/sumin/dev/ios/whiskey-note/docs/superpowers/icon/1024.png"
```

글라스 형태와 색상이 올바른지 확인. 문제 있으면 SVG 수정 → Task 2 재실행.

---

### Task 3: 에셋 카탈로그 교체

**Files:**
- Modify: `whiskey note/Assets.xcassets/AppIcon.appiconset/*.png` (11개)

- [ ] **Step 1: 기존 파일 백업**

```bash
cp -r "/Users/sumin/dev/ios/whiskey-note/whiskey note/Assets.xcassets/AppIcon.appiconset" \
      "/Users/sumin/dev/ios/whiskey-note/docs/superpowers/icon/backup"
echo "Backup done"
```

- [ ] **Step 2: 새 PNG 복사**

```bash
ICON_DIR="/Users/sumin/dev/ios/whiskey-note/docs/superpowers/icon"
ASSET_DIR="/Users/sumin/dev/ios/whiskey-note/whiskey note/Assets.xcassets/AppIcon.appiconset"

for size in 1024 180 120 87 80 60 58 57 40 29 114; do
  cp "${ICON_DIR}/${size}.png" "${ASSET_DIR}/${size}.png"
  echo "Copied ${size}.png"
done
```

- [ ] **Step 3: 파일 확인**

```bash
ls -la "/Users/sumin/dev/ios/whiskey-note/whiskey note/Assets.xcassets/AppIcon.appiconset/"
```

Expected: 11개 PNG + Contents.json. 타임스탬프가 방금 것으로 갱신됐는지 확인.

- [ ] **Step 4: Xcode에서 시각 확인**

Xcode에서 `whiskey note/Assets.xcassets`를 열어 AppIcon이 새 디자인으로 표시되는지 확인.

- [ ] **Step 5: 커밋**

```bash
cd "/Users/sumin/dev/ios/whiskey-note"
git add "whiskey note/Assets.xcassets/AppIcon.appiconset/"
git add docs/superpowers/icon/icon-master.svg
git add docs/superpowers/icon/generate.sh
git add docs/superpowers/icon/*.png
git add docs/superpowers/plans/2026-03-23-app-icon.md
git add docs/superpowers/specs/2026-03-23-app-icon-design.md
git commit -m "feat: redesign app icon — Glencairn glass, dark brown bg"
```

---

### 롤백 방법

문제 발생 시 백업에서 복원:

```bash
cp "/Users/sumin/dev/ios/whiskey-note/docs/superpowers/icon/backup/"*.png \
   "/Users/sumin/dev/ios/whiskey-note/whiskey note/Assets.xcassets/AppIcon.appiconset/"
```
